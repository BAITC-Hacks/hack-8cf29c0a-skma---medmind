import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';

export function SalesTrend({ data }: { data: { date: string; operations: number }[] }) {
  return <section className="min-w-0 rounded-3xl border border-border bg-surface p-5">
    <h2 className="text-lg font-semibold">Динамика операций</h2><p className="mt-1 text-xs text-text-secondary">Количество строк документов по дням · по выбранным фильтрам</p>
    {data.length ? <><div className="mt-5 h-52 w-full"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} accessibilityLayer><CartesianGrid stroke="var(--border)" vertical={false} /><XAxis dataKey="date" tick={{ fill: 'var(--text-secondary)', fontSize: 11 }} minTickGap={40} /><YAxis tick={{ fill: 'var(--text-secondary)', fontSize: 11 }} /><Tooltip contentStyle={{ background: 'var(--surface)', border: '1px solid var(--border)', color: 'var(--text-primary)' }} itemStyle={{ color: 'var(--text-primary)' }} cursor={{ fill: 'var(--page-bg)' }} /><Bar name="Операций" dataKey="operations" fill="var(--chart-series-1)" isAnimationActive={false} /></BarChart></ResponsiveContainer></div><details className="mt-4 text-sm"><summary className="cursor-pointer rounded focus-visible:ring-2 focus-visible:ring-accent-focus-ring">Показать значения</summary><div className="mt-3 max-h-52 overflow-auto"><table className="w-full text-left tabular-nums"><thead><tr><th>Дата</th><th>Операций</th></tr></thead><tbody>{data.map(row => <tr key={row.date} className="border-t border-border"><td className="py-2">{row.date}</td><td>{row.operations}</td></tr>)}</tbody></table></div></details></> : <p className="py-12 text-center text-text-secondary">Нет данных для графика</p>}
  </section>;
}
